declare type Page = {
    name: string;
    projectId: string;
    path: string;
    url: string;
};
export declare function generateNextPages(pages: Page[], dir: string, config: any): Promise<void>;
export {};
